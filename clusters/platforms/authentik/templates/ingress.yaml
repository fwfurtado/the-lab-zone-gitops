---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik-server
  namespace: authentik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`auth.platform.the-lab.zone`)
      kind: Rule
      services:
        - name: authentik-server
          port: 80
